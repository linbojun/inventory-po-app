# Database Configuration
# For SQLite (default):
DATABASE_URL=sqlite:///./inventory_po.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/inventory_po

# Upload Directory
# Directory where uploaded files (Excel, PDF) are temporarily stored
UPLOAD_DIR=./uploads

# Image Directory
# Directory where product images are stored
IMAGE_DIR=./static/images
