# Database Configuration
# For SQLite (default):
DATABASE_URL=sqlite:///./inventory_po.db
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/inventory_po

# Upload Directory
# Directory where uploaded files (Excel, PDF) are temporarily stored
UPLOAD_DIR=./uploads

# Image Directory
# Directory where product images are stored
IMAGE_DIR=./static/images

# CORS configuration
# Override the full allowlist (comma separated) only if you need to restrict it.
# CORS_ALLOW_ORIGINS=https://inventory-po-app.vercel.app,https://your-custom-domain.com

# Append additional origins without losing defaults (localhost + inventory-po-app.vercel.app).
# CORS_EXTRA_ORIGINS=https://staging.your-domain.com

# Keep Vercel preview deployments (and localhost with random ports) allowed by regex.
# Set to false only if you need to disable *.vercel.app previews entirely.
ENABLE_VERCEL_PREVIEW_CORS=true

